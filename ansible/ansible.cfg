[defaults]
inventory        = inventory.ini
remote_user      = ubuntu
private_key_file = ~/.ssh/expense-tracker-key
host_key_checking = False
stdout_callback  = yaml
timeout          = 30

[privilege_escalation]
become      = True
become_method = sudo